# Open-Meteo Research Summary## OverviewSource research findings for WO-WEATHER-R. Verifies Open-Meteo as the primary current-build weather source for GOD EYES Layer 07.---## Final Findings### Endpoint Confirmed| Attribute | Finding ||-----------|---------|| Base URL | `https://api.open-meteo.com` || Endpoint | `/v1/forecast` || Method | GET || Full URL | `GET https://api.open-meteo.com/v1/forecast` |### Authentication Confirmed| Attribute | Finding ||-----------|---------|| API Key Required (Free) | **No** || API Key Required (Commercial) | **Yes** (paid subscription, `customer-` URL prefix) || User-Agent Required | No (not enforced) |### Licence Confirmed| Attribute | Finding ||-----------|---------|| Data Licence | CC-BY 4.0 || Attribution Required | Yes — link to open-meteo.com with "Weather data by Open-Meteo.com" || Source Code Licence | AGPLv3 || Commercial Use (Free Tier) | Prohibited |### Limits Confirmed| Attribute | Finding ||-----------|---------|| Calls per day | 10,000 || Calls per hour | 5,000 || Calls per minute | 600 || Calls per month | 300,000 (free tier) |---## Confirmed Assumptions| Planning Assumption | Official Finding | Status ||--------------------|--------------------|--------|| No API key for free use | Confirmed | MATCH || 10,000 calls/day limit | Confirmed | MATCH || Global WGS84 coverage | Confirmed | MATCH || Multiple coordinates per request | Confirmed | MATCH || CC-BY 4.0 licence | Confirmed | MATCH || `temperature_2m` available | Confirmed | MATCH || `apparent_temperature` available | Confirmed | MATCH || `wind_speed_10m` available | Confirmed | MATCH || `wind_direction_10m` available | Confirmed | MATCH || `wind_gusts_10m` available | Confirmed | MATCH || `relative_humidity_2m` available | Confirmed | MATCH || `pressure_msl` available | Confirmed | MATCH || `surface_pressure` available | Confirmed | MATCH || `precipitation` available | Confirmed | MATCH || `precipitation_probability` available | Confirmed (hourly only) | MATCH || `cloud_cover` available | Confirmed | MATCH || `weather_code` available | Confirmed | MATCH || `cell_selection=land` parameter | Confirmed | MATCH || `timezone=auto` parameter | Confirmed | MATCH || `forecast_days` parameter | Confirmed (0-16) | MATCH || WMO weather codes used | Confirmed | MATCH || Response includes elevation | Confirmed (90m DEM) | MATCH || Response includes generationtime_ms | Confirmed | MATCH || Response includes timezone info | Confirmed | MATCH || Response includes utc_offset_seconds | Confirmed | MATCH |---## Corrected Assumptions| Planning Assumption | Official Finding | Correction Needed ||--------------------|--------------------|-------------------|| `precipitation_probability` in current | **Not available in current block** | Already handled — planning docs show NULL for current || `surface_pressure` as primary pressure | Both `pressure_msl` and `surface_pressure` available | `pressure_msl` for primary display, `surface_pressure` in metadata || Weather code labels from API | **API returns numeric codes only** | GOD EYES must implement WMO code-to-label mapping || Grid cell center = requested point | **Returned lat/lon may differ from requested** | Already documented in planning — grid cell center, not exact point |---## Unresolved Questions| Question | Status | Resolution Path ||----------|--------|-----------------|| What counts as 1 API call? | **UNRESOLVED** | Must be measured in WO-WEATHER-S fetch proof || Are rate-limit headers returned? | **UNRESOLVED** | Must be observed in WO-WEATHER-S fetch proof || Does batch size affect call counting? | **UNRESOLVED** | Must be measured in WO-WEATHER-S fetch proof || Does requesting current+hourly count as 1 or 2 calls? | **UNRESOLVED** | Must be measured in WO-WEATHER-S fetch proof || Maximum practical batch size? | **UNRESOLVED** | Must be tested in WO-WEATHER-S fetch proof || Actual rate-limit error behavior? | **UNRESOLVED** | Must be observed in WO-WEATHER-S fetch proof |---## API-Call Estimate Analysis### Planning Estimate- ~216 calls/day (54 calls per fetch x 4 fetches per day)- Based on 5° grid (2,664 cells) with 50 cells per batch### Official Information- Rate limits: 10,000/day, 5,000/hour, 600/minute- No documentation on how requests are counted- No documentation on batch size impact on counting### Risk Assessment| Scenario | Impact | Likelihood ||----------|--------|------------|| 1 request = 1 call (regardless of coords) | Estimate is accurate | Medium || 1 request = N calls (per coordinate) | Estimate could be 50x higher | Low (but must verify) || Current+hourly = 2 calls | Estimate could be 2x higher | Low (but must verify) || forecast_days affects counting | Estimate could vary | Unknown |### RecommendationThe ~216 calls/day estimate is reasonable as a planning figure but **must be verified** in WO-WEATHER-S. The fetch proof should:1. Record HTTP response headers for rate-limit information2. Track actual call counts vs expected3. Test with varying batch sizes4. Test with current-only vs current+hourly requests5. Document safe batch size and calling patterns---## Recommendation for WO-WEATHER-S Fetch Proof### Pre-Fetch Checklist1. Confirm endpoint URL: `GET https://api.open-meteo.com/v1/forecast`2. Confirm no API key needed (do NOT send any key parameter)3. Prepare 5 test coordinates (Berlin, New York, Tokyo, Sydney, Nairobi)4. Prepare request with all 11 current + 12 hourly variables5. Set `forecast_days=3`, `timezone=auto`, `cell_selection=land`### During Fetch1. Record all HTTP response headers2. Record response body3. Record response time4. Check for rate-limit headers (`X-RateLimit-*`, `Retry-After`, etc.)5. Validate response structure matches expected shape6. Check returned lat/lon vs requested lat/lon### Post-Fetch Validation1. Verify HTTP 200 status2. Verify response is JSON array (5 elements)3. Verify all requested variables present in `current` and `hourly`4. Verify weather codes are valid WMO codes5. Verify temperature values are reasonable (-90 to +60 °C)6. Verify humidity values are 0-100 %7. Verify wind direction values are 0-360 °8. Document any discrepancies from planning docs9. Save raw response to `raw/layer_07_weather/open-meteo/...`### Proof Output- `metadata.json` — fetch run metadata- `proof_response.json` — raw API response- `preview.json` — first coordinate with all fields- `observed_fields.json` — all fields found in response- `proof_report.md` — human-readable report with validation results