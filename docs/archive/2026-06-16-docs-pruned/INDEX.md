# 2026-06-16 Docs Pruned Archive

Classification: ARCHIVE_INDEX

This archive batch preserves documentation that was useful as evidence or placeholder
structure, but should not appear in the active documentation tree.

## Contents

| Path | Reason archived |
|------|-----------------|
| `retired-doc-stubs/api/` | Placeholder-only API docs folder. Active API structure rules live in `docs/control/PROJECT_RULES.md` and contracts live in `packages/contracts/`. |
| `retired-doc-stubs/data/` | Placeholder-only data docs folder. Active data rules live in `docs/control/PROJECT_RULES.md` and `docs/control/LAYER_AND_DATA_CONTRACT.md`. |
| `retired-doc-stubs/work-orders/` | Placeholder-only work-order folder. Recreate `docs/work-orders/` only when an active small work order exists. |
| `spec-008-evidence/` | Bulky research, graph, API review, and compression-plan evidence for Spec 008. The active spec spine remains under `specs/008-structure-remediation-roadmap/`. |

Archived files are historical evidence only. They do not override `AGENTS.md`,
`.specify/memory/constitution.md`, or active files under `docs/control/`.
